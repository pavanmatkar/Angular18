import { UnsavedChangesDirective } from './unsaved-changes.directive';

describe('UnsavedChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new UnsavedChangesDirective();
    expect(directive).toBeTruthy();
  });
});
